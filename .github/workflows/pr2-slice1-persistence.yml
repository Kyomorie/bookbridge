name: PR2 slice 1 persistence

on:
  push:
    branches:
      - feature/kosync-abs-rewind-approval

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Fail-closed repository preflight
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "25799bad36f7493e4b61a88e15a8ffc58e278b60"
          git fetch --no-tags https://github.com/cporcellijr/bookbridge.git dev:refs/remotes/upstream/dev
          test "$(git rev-parse refs/remotes/upstream/dev)" = "6afbdc68de3e67298d0def330e0d15cf8c6781e2"
          test -z "$(git status --porcelain)"
      - name: Install test dependencies
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update >/dev/null
          sudo apt-get install -y lua5.4 >/dev/null
          python -m pip install --upgrade pip >/dev/null
          pip install flake8 pytest pytest-cov >/dev/null
          pip install -r requirements.txt >/dev/null
      - name: Verify migration head
        shell: bash
        run: |
          set -euo pipefail
          mapfile -t HEADS < <(alembic heads | sed '/^[[:space:]]*$/d')
          test "${#HEADS[@]}" -eq 1
          [[ "${HEADS[0]}" == 2e0a47a3dadd* ]]
      - name: Apply slice 1
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{path}: expected exactly one match, got {count}: {old[:80]!r}")
              p.write_text(text.replace(old, new, 1))

          # Model: one durable row per (user, ABS item, exact KoSync source snapshot).
          model = '''class PendingRewind(Base):
    """Durable user decision for a proposed KoSync -> ABS backward write.

    ``source_fingerprint`` is a SHA-256 of the canonical source snapshot and is
    unique per user/book. Terminal rows deliberately remain in the table so a
    dismissed or expired source snapshot cannot nag again every sync cycle.
    ``target_snapshot_json`` is stored separately and must be revalidated before
    an approval is allowed to write anything to Audiobookshelf.
    """
    __tablename__ = 'pending_rewinds'

    id = Column(Integer, primary_key=True, autoincrement=True)
    user_id = Column(Integer, ForeignKey('users.id', ondelete='CASCADE'), nullable=False, index=True)
    abs_id = Column(String(255), ForeignKey('books.abs_id', ondelete='CASCADE'), nullable=False, index=True)
    source = Column(String(32), nullable=False, default='KoSync')
    source_fingerprint = Column(String(64), nullable=False)
    source_snapshot_json = Column(Text, nullable=False)
    target_snapshot_json = Column(Text, nullable=False)
    proposed_timestamp = Column(Float, nullable=False)
    proposed_percentage = Column(Float, nullable=False)
    status = Column(String(20), nullable=False, default='pending', index=True)
    created_at = Column(DateTime, nullable=False, default=utcnow)
    expires_at = Column(DateTime, nullable=False)
    resolved_at = Column(DateTime, nullable=True)

    __table_args__ = (
        Index(
            'ix_pending_rewinds_user_abs_source',
            'user_id', 'abs_id', 'source_fingerprint',
            unique=True,
        ),
        Index('ix_pending_rewinds_user_status_expiry', 'user_id', 'status', 'expires_at'),
    )

    def __init__(
        self,
        user_id: int,
        abs_id: str,
        source_fingerprint: str,
        source_snapshot_json: str,
        target_snapshot_json: str,
        proposed_timestamp: float,
        proposed_percentage: float,
        expires_at: datetime,
        source: str = 'KoSync',
        status: str = 'pending',
    ):
        self.user_id = user_id
        self.abs_id = abs_id
        self.source = source
        self.source_fingerprint = source_fingerprint
        self.source_snapshot_json = source_snapshot_json
        self.target_snapshot_json = target_snapshot_json
        self.proposed_timestamp = proposed_timestamp
        self.proposed_percentage = proposed_percentage
        self.status = status
        self.created_at = utcnow()
        self.expires_at = expires_at

    @staticmethod
    def _json_dict(raw: str) -> dict:
        import json
        try:
            parsed = json.loads(raw) if raw else {}
            return parsed if isinstance(parsed, dict) else {}
        except (TypeError, json.JSONDecodeError):
            return {}

    @property
    def source_snapshot(self) -> dict:
        return self._json_dict(self.source_snapshot_json)

    @property
    def target_snapshot(self) -> dict:
        return self._json_dict(self.target_snapshot_json)

    def __repr__(self):
        return (
            f"<PendingRewind(id={self.id}, user_id={self.user_id}, "
            f"abs_id='{self.abs_id}', status='{self.status}')>"
        )


'''
          replace_once('src/db/models.py', 'class PendingSuggestion(Base):\n', model + 'class PendingSuggestion(Base):\n')

          replace_once(
              'src/db/database_service.py',
              '    PendingSuggestion,\n',
              '    PendingSuggestion,\n    PendingRewind,\n',
          )

          methods = '''    # Pending rewind approval operations (KoSync -> ABS only)
    @staticmethod
    def _rewind_snapshot_json(snapshot: dict) -> str:
        """Canonical JSON used both for persistence and source fingerprinting."""
        if not isinstance(snapshot, dict):
            raise ValueError("rewind snapshot must be a dict")
        return json.dumps(snapshot, sort_keys=True, separators=(",", ":"), ensure_ascii=False)

    @classmethod
    def _rewind_source_fingerprint(cls, source_snapshot: dict) -> str:
        import hashlib
        payload = cls._rewind_snapshot_json(source_snapshot).encode("utf-8")
        return hashlib.sha256(payload).hexdigest()

    @staticmethod
    def _serialize_pending_rewind(row: PendingRewind) -> dict:
        return {
            "id": row.id,
            "user_id": row.user_id,
            "abs_id": row.abs_id,
            "source": row.source,
            "source_fingerprint": row.source_fingerprint,
            "source_snapshot": row.source_snapshot,
            "target_snapshot": row.target_snapshot,
            "proposed_timestamp": row.proposed_timestamp,
            "proposed_percentage": row.proposed_percentage,
            "status": row.status,
            "created_at": row.created_at,
            "expires_at": row.expires_at,
            "resolved_at": row.resolved_at,
        }

    @staticmethod
    def _expire_rewind_row(row: PendingRewind, now: datetime) -> bool:
        if row.status == "pending" and row.expires_at is not None and row.expires_at <= now:
            row.status = "expired"
            row.resolved_at = now
            return True
        return False

    def get_or_create_pending_rewind(
        self,
        user_id: int,
        abs_id: str,
        source_snapshot: dict,
        target_snapshot: dict,
        proposed_timestamp: float,
        proposed_percentage: float,
        ttl_hours: float,
        now: datetime = None,
    ) -> tuple[dict, bool]:
        """Create one durable pending rewind, or return the prior source decision.

        Dedupe deliberately spans all statuses. Once a source snapshot is
        dismissed or expires it cannot reappear until KoSync actually moves and
        therefore produces a different fingerprint.
        """
        if user_id is None or not abs_id:
            raise ValueError("user_id and abs_id are required for pending rewinds")
        try:
            ttl = float(ttl_hours)
        except (TypeError, ValueError) as exc:
            raise ValueError("ttl_hours must be numeric") from exc
        if ttl <= 0:
            raise ValueError("ttl_hours must be greater than zero")

        source_json = self._rewind_snapshot_json(source_snapshot)
        target_json = self._rewind_snapshot_json(target_snapshot)
        source_fingerprint = self._rewind_source_fingerprint(source_snapshot)
        now = now or utcnow()
        expires_at = now + timedelta(hours=ttl)

        with self.get_session() as session:
            existing = session.query(PendingRewind).filter(
                PendingRewind.user_id == user_id,
                PendingRewind.abs_id == abs_id,
                PendingRewind.source_fingerprint == source_fingerprint,
            ).first()
            if existing is not None:
                self._expire_rewind_row(existing, now)
                session.flush()
                return self._serialize_pending_rewind(existing), False

            row = PendingRewind(
                user_id=user_id,
                abs_id=abs_id,
                source_fingerprint=source_fingerprint,
                source_snapshot_json=source_json,
                target_snapshot_json=target_json,
                proposed_timestamp=float(proposed_timestamp),
                proposed_percentage=float(proposed_percentage),
                expires_at=expires_at,
            )
            session.add(row)
            session.flush()
            return self._serialize_pending_rewind(row), True

    def get_pending_rewinds(self, user_id: int, now: datetime = None) -> list[dict]:
        """Return only live pending requests for one explicit user."""
        if user_id is None:
            return []
        now = now or utcnow()
        with self.get_session() as session:
            rows = session.query(PendingRewind).filter(
                PendingRewind.user_id == user_id,
                PendingRewind.status == "pending",
            ).order_by(PendingRewind.created_at.asc(), PendingRewind.id.asc()).all()
            live = []
            for row in rows:
                if self._expire_rewind_row(row, now):
                    continue
                live.append(self._serialize_pending_rewind(row))
            return live

    def get_pending_rewind(self, user_id: int, request_id: int, now: datetime = None) -> Optional[dict]:
        """Fetch one live request, never crossing the caller's user boundary."""
        if user_id is None or request_id is None:
            return None
        now = now or utcnow()
        with self.get_session() as session:
            row = session.query(PendingRewind).filter(
                PendingRewind.id == request_id,
                PendingRewind.user_id == user_id,
            ).first()
            if row is None:
                return None
            if self._expire_rewind_row(row, now):
                return None
            if row.status != "pending":
                return None
            return self._serialize_pending_rewind(row)

    def resolve_pending_rewind(
        self,
        user_id: int,
        request_id: int,
        status: str,
        now: datetime = None,
    ) -> bool:
        """Move a live request to a terminal state for the same explicit user."""
        if status not in {"accepted", "dismissed", "stale"}:
            raise ValueError(f"invalid pending rewind status: {status}")
        if user_id is None or request_id is None:
            return False
        now = now or utcnow()
        with self.get_session() as session:
            row = session.query(PendingRewind).filter(
                PendingRewind.id == request_id,
                PendingRewind.user_id == user_id,
            ).first()
            if row is None or row.status != "pending":
                return False
            if self._expire_rewind_row(row, now):
                return False
            row.status = status
            row.resolved_at = now
            return True

'''
          replace_once(
              'src/db/database_service.py',
              '    # PendingSuggestion operations\n',
              methods + '    # PendingSuggestion operations\n',
          )

          replace_once(
              'src/utils/config_loader.py',
              "    'KOSYNC_XPATH_ORDER_ENABLED', 'KOSYNC_FURTHEST_WINS',\n",
              "    'KOSYNC_XPATH_ORDER_ENABLED', 'KOSYNC_FURTHEST_WINS',\n    'KOSYNC_ABS_REWIND_TTL_HOURS',\n",
          )
          replace_once(
              'src/utils/config_loader.py',
              "    'KOSYNC_FURTHEST_WINS': 'true',\n",
              "    'KOSYNC_FURTHEST_WINS': 'true',\n    'KOSYNC_ABS_REWIND_TTL_HOURS': '24',\n",
          )

          migration = Path('alembic/versions/b4d7e9f2c1a6_add_pending_rewinds.py')
          if migration.exists():
              raise SystemExit('migration path already exists')
          migration.write_text('''"""add durable pending KoSync -> ABS rewind approvals

Revision ID: b4d7e9f2c1a6
Revises: 2e0a47a3dadd
Create Date: 2026-08-29
"""

from alembic import op
import sqlalchemy as sa

revision = "b4d7e9f2c1a6"
down_revision = "2e0a47a3dadd"
branch_labels = None
depends_on = None

_TABLE = "pending_rewinds"


def upgrade() -> None:
    bind = op.get_bind()
    inspector = sa.inspect(bind)
    if _TABLE in set(inspector.get_table_names()):
        return

    op.create_table(
        _TABLE,
        sa.Column("id", sa.Integer(), primary_key=True, autoincrement=True),
        sa.Column("user_id", sa.Integer(), sa.ForeignKey("users.id", ondelete="CASCADE"), nullable=False),
        sa.Column("abs_id", sa.String(length=255), sa.ForeignKey("books.abs_id", ondelete="CASCADE"), nullable=False),
        sa.Column("source", sa.String(length=32), nullable=False, server_default="KoSync"),
        sa.Column("source_fingerprint", sa.String(length=64), nullable=False),
        sa.Column("source_snapshot_json", sa.Text(), nullable=False),
        sa.Column("target_snapshot_json", sa.Text(), nullable=False),
        sa.Column("proposed_timestamp", sa.Float(), nullable=False),
        sa.Column("proposed_percentage", sa.Float(), nullable=False),
        sa.Column("status", sa.String(length=20), nullable=False, server_default="pending"),
        sa.Column("created_at", sa.DateTime(), nullable=False),
        sa.Column("expires_at", sa.DateTime(), nullable=False),
        sa.Column("resolved_at", sa.DateTime(), nullable=True),
    )
    op.create_index("ix_pending_rewinds_user_id", _TABLE, ["user_id"], unique=False)
    op.create_index("ix_pending_rewinds_abs_id", _TABLE, ["abs_id"], unique=False)
    op.create_index("ix_pending_rewinds_status", _TABLE, ["status"], unique=False)
    op.create_index(
        "ix_pending_rewinds_user_abs_source",
        _TABLE,
        ["user_id", "abs_id", "source_fingerprint"],
        unique=True,
    )
    op.create_index(
        "ix_pending_rewinds_user_status_expiry",
        _TABLE,
        ["user_id", "status", "expires_at"],
        unique=False,
    )


def downgrade() -> None:
    bind = op.get_bind()
    inspector = sa.inspect(bind)
    if _TABLE in set(inspector.get_table_names()):
        op.drop_table(_TABLE)
''')

          Path('tests/test_pending_rewinds.py').write_text('''from datetime import datetime, timedelta

from sqlalchemy import inspect

from src.db.database_service import DatabaseService
from src.db.models import Book
from src.utils.config_loader import ALL_SETTINGS, DEFAULT_CONFIG, KNOWN_SETTING_KEYS


def _service(tmp_path):
    db = DatabaseService(str(tmp_path / "rewinds.db"))
    user1 = db.create_user("reader-one", role="admin")
    user2 = db.create_user("reader-two")
    db.create_book(Book(abs_id="abs-book", abs_title="Book", duration=1000))
    db.link_user_book(user1.id, "abs-book")
    db.link_user_book(user2.id, "abs-book")
    return db, user1, user2


def _source(pct=0.55, xpath="/body/DocFragment[5]"):
    return {
        "client": "KoSync",
        "pct": pct,
        "xpath": xpath,
        "device": "kindle",
        "device_id": "k1",
        "service_updated_at": 1234.0,
    }


def _target(pct=0.79, ts=790.0):
    return {"client": "ABS", "pct": pct, "ts": ts, "service_updated_at": 1200.0}


def test_pending_rewind_schema_and_setting_are_registered(tmp_path):
    db, _, _ = _service(tmp_path)
    assert "pending_rewinds" in inspect(db.db_manager.engine).get_table_names()
    assert "KOSYNC_ABS_REWIND_TTL_HOURS" in ALL_SETTINGS
    assert "KOSYNC_ABS_REWIND_TTL_HOURS" in KNOWN_SETTING_KEYS
    assert DEFAULT_CONFIG["KOSYNC_ABS_REWIND_TTL_HOURS"] == "24"


def test_pending_rewind_round_trips_source_and_target_snapshots(tmp_path):
    db, user, _ = _service(tmp_path)
    now = datetime(2026, 8, 29, 12, 0, 0)
    row, created = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )
    assert created is True
    assert row["source_snapshot"] == _source()
    assert row["target_snapshot"] == _target()
    assert row["proposed_timestamp"] == 550.0
    assert row["proposed_percentage"] == 0.55
    assert row["status"] == "pending"
    assert row["expires_at"] == now + timedelta(hours=24)


def test_dedupe_survives_dismiss_and_only_source_movement_creates_new_request(tmp_path):
    db, user, _ = _service(tmp_path)
    now = datetime(2026, 8, 29, 12, 0, 0)
    first, created = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )
    assert created is True
    assert db.resolve_pending_rewind(user.id, first["id"], "dismissed", now=now) is True

    same, created = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(pct=0.82, ts=820.0), 550.0, 0.55, 24, now=now
    )
    assert created is False
    assert same["id"] == first["id"]
    assert same["status"] == "dismissed"
    # Dedupe is intentionally source-bound; a newer ABS target alone does not nag again.
    assert same["target_snapshot"] == _target()

    moved, created = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(pct=0.56), _target(pct=0.82, ts=820.0), 560.0, 0.56, 24, now=now
    )
    assert created is True
    assert moved["id"] != first["id"]


def test_pending_rewinds_are_strictly_per_user(tmp_path):
    db, user1, user2 = _service(tmp_path)
    now = datetime(2026, 8, 29, 12, 0, 0)
    r1, _ = db.get_or_create_pending_rewind(
        user1.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )
    r2, created = db.get_or_create_pending_rewind(
        user2.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )
    assert created is True
    assert r1["id"] != r2["id"]
    assert [r["id"] for r in db.get_pending_rewinds(user1.id, now=now)] == [r1["id"]]
    assert [r["id"] for r in db.get_pending_rewinds(user2.id, now=now)] == [r2["id"]]
    assert db.get_pending_rewind(user1.id, r2["id"], now=now) is None
    assert db.resolve_pending_rewind(user1.id, r2["id"], "dismissed", now=now) is False


def test_expiry_is_fail_closed_and_deduped(tmp_path):
    db, user, _ = _service(tmp_path)
    created_at = datetime(2026, 8, 29, 12, 0, 0)
    first, _ = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 1, now=created_at
    )
    after_expiry = created_at + timedelta(hours=2)
    assert db.get_pending_rewind(user.id, first["id"], now=after_expiry) is None
    assert db.get_pending_rewinds(user.id, now=after_expiry) == []
    assert db.resolve_pending_rewind(user.id, first["id"], "accepted", now=after_expiry) is False

    same, created = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=after_expiry
    )
    assert created is False
    assert same["id"] == first["id"]
    assert same["status"] == "expired"


def test_pending_rewind_persists_across_service_restart(tmp_path):
    db_path = tmp_path / "restart.db"
    db = DatabaseService(str(db_path))
    user = db.create_user("reader", role="admin")
    db.create_book(Book(abs_id="abs-book", abs_title="Book", duration=1000))
    db.link_user_book(user.id, "abs-book")
    now = datetime(2026, 8, 29, 12, 0, 0)
    row, _ = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )

    restarted = DatabaseService(str(db_path))
    loaded = restarted.get_pending_rewind(user.id, row["id"], now=now + timedelta(minutes=5))
    assert loaded is not None
    assert loaded["status"] == "pending"
    assert loaded["source_snapshot"] == _source()
    assert loaded["target_snapshot"] == _target()


def test_invalid_ttl_and_terminal_transition_fail_closed(tmp_path):
    db, user, _ = _service(tmp_path)
    now = datetime(2026, 8, 29, 12, 0, 0)
    for ttl in (0, -1, "not-a-number"):
        try:
            db.get_or_create_pending_rewind(
                user.id, "abs-book", _source(), _target(), 550.0, 0.55, ttl, now=now
            )
        except ValueError:
            pass
        else:
            raise AssertionError(f"ttl={ttl!r} must fail closed")

    row, _ = db.get_or_create_pending_rewind(
        user.id, "abs-book", _source(), _target(), 550.0, 0.55, 24, now=now
    )
    assert db.resolve_pending_rewind(user.id, row["id"], "dismissed", now=now) is True
    assert db.resolve_pending_rewind(user.id, row["id"], "accepted", now=now) is False
''')
          PY
      - name: Validate slice 1
        shell: bash
        run: |
          set -euo pipefail
          alembic heads
          test "$(alembic heads | sed '/^[[:space:]]*$/d' | wc -l)" -eq 1
          alembic heads | grep -q '^b4d7e9f2c1a6'
          flake8 src/db/models.py src/db/database_service.py src/utils/config_loader.py tests/test_pending_rewinds.py --count --select=E9,F63,F7,F82 --show-source --statistics
          pytest -q tests/test_pending_rewinds.py
          pytest -q tests/test_database_service_integration.py tests/test_settings_comprehensive.py tests/test_settings_save_key_allowlist.py tests/test_settings_taxonomy.py
      - name: Commit only validated slice
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin feature/kosync-abs-rewind-approval
          test "$(git rev-parse origin/feature/kosync-abs-rewind-approval)" = "25799bad36f7493e4b61a88e15a8ffc58e278b60"
          git fetch --no-tags https://github.com/cporcellijr/bookbridge.git dev:refs/remotes/upstream/dev
          test "$(git rev-parse refs/remotes/upstream/dev)" = "6afbdc68de3e67298d0def330e0d15cf8c6781e2"
          rm -f .github/workflows/pr2-slice1-persistence.yml
          git config user.name "Kyomorie"
          git config user.email "246901271+Kyomorie@users.noreply.github.com"
          git add src/db/models.py src/db/database_service.py src/utils/config_loader.py alembic/versions/b4d7e9f2c1a6_add_pending_rewinds.py tests/test_pending_rewinds.py .github/workflows/pr2-slice1-persistence.yml
          git commit -m "[Feature] Persist pending KoSync ABS rewind decisions"
          git push origin HEAD:feature/kosync-abs-rewind-approval
